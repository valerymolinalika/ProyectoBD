import React from 'react';
import SideBarMenu from '../../components/SideBarMenu';

const Subjects = () => {
  return (
    <main className="flex ml-16 bg-blue-500 w-screen min-h-screen flex items-center justify-center">
      <SideBarMenu />
      <div>Subjects</div>
    </main>
  )
}

export default Subjects